[project]
name = "anki-george-german"
version = "0.1.0"
description = "German vocabulary Anki deck tools"
requires-python = ">=3.13"
dependencies = [
    "argcomplete>=3.0",
    "de-dep-news-trf @ https://github.com/explosion/spacy-models/releases/download/de_dep_news_trf-3.8.0/de_dep_news_trf-3.8.0.tar.gz",
    "rapidfuzz>=3.14.3",
    "requests>=2.32.5",
    "spacy[transformers]>=3.7",
    "charsplit @ git+https://github.com/dtuggener/CharSplit.git",
    "torch>=2.0",
    "spacy-transformers>=1.3",
    "pip>=26.0.1",
]

[project.scripts]
anki-german = "anki_george_german.cli:main"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=6.0",
    "responses>=0.25",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "slow: tests requiring spaCy model load",
]
addopts = "--cov=anki_george_german --cov-report=term-missing"

[tool.coverage.run]
source = ["anki_george_german"]
omit = [
    "anki_george_german/generate_vocab.py",
    "anki_george_german/chapters.py",
]

[tool.coverage.report]
show_missing = true
skip_empty = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true
